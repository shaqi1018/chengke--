# LSM 6664Hz 丢帧问题 — 定位结论

**日期**：2026-06-17
**结论**：丢帧根因在**新固件（4 端点带麦克风版）**，与上位机、WinUSB、USB FS 带宽**均无关**。

---

## 一、问题回顾

新固件 + 上位机采集 LSM @ 6664Hz 时，稳定丢帧 **17~18%**。
此前曾沿"上位机/WinUSB 慢"方向排查良久（overlapped、多线程、RAW_IO、禁用 USB 选择性挂起、迁移 libusb…），均无改善。

---

## 二、决定性对照实验

固件团队今天把 `Sensor_Proj_V1.0` 源码切回**旧版（3 端点、无麦克风）**并烧录。
我们用同一台 Windows、同一根线、同一上位机代码，做了端点映射适配（旧固件 RESP=0x84、无 MIC），对照测试：

| # | 固件 | 上位机 | LSM 6664Hz 丢帧率 | 吞吐 |
|---|------|--------|------------------|------|
| 1 | 旧固件 3 端点 | publish 旧版 | **0.41%** ✅ | — |
| 2 | **旧固件 3 端点** | **新版 WinUSB（本次）** | **0.001%（132386 帧丢 1 帧）** ✅✅ | **383–390 KB/s 满速** |
| 3 | 新固件 4 端点 | 新版（开麦克风） | 17~18% ❌ | — |
| 4 | 新固件 4 端点 | 新版（**关麦克风**） | 17% ❌ | — |

> 测试 2 录制：`Recordings/20260617_213147/`，吞吐稳定 388 KB/s（理论需求 377.5 KB/s）。

---

## 三、结论推导

对照组 #2 与 #3/#4 的**唯一变量是固件**（上位机、驱动、机器、线缆全相同）：

1. ✅ **上位机代码无问题** — 新版 WinUSB 接旧固件跑出 0.001%，优于旧 publish 上位机。
2. ✅ **WinUSB 无问题** — 实测满速 388 KB/s。
3. ✅ **USB FS 带宽够** — 386 KB/s 仅占 FS 实际可用（~1.1 MB/s）的 ~35%；印证固件团队 `REFUTE_BANDWIDTH.md`。
4. ✅ **不是麦克风带宽抢占** — 测试 #4 关麦克风仍 17%。
5. ❌ **根因 = 新固件（3→4 端点改造）引入的回归** — 是 4 端点版本代码本身的问题，与是否实际推流麦克风无关。

之前所有"上位机/WinUSB 慢"的判断**全部推翻**。

---

## 四、给固件团队的排查方向

请用**真正的新固件（4 端点带麦克风）源码**与旧版（3 端点）逐项 diff，重点查：

### 1. SOF 发送循环边界（最可疑）
`usbd_wcid_streaming.c : USBD_WCID_STREAMING_SOF()`
```c
for (i = 0; i < (N_IN_ENDPOINTS - 1U); i++) { ... 发送 ch i ... }   // 传感器通道
if (TX_States[N_IN_ENDPOINTS - 1U] != 1U) {                         // 最后一个通道(tag/mic)单独处理
    for (i = (N_IN_ENDPOINTS - 1U); i < N_CHANNELS_MAX; i++) { ... }
}
```
`N_IN_ENDPOINTS` 由 3→4 后，循环把 LSM(ch0) 归入前半段、麦克风归入"最后通道"特殊分支。
**需确认**：4 端点下 LSM(ch0) 的 `TX_States`/`TxBuffStatus` 是否被新加的麦克风分支逻辑影响、是否每个 SOF 仍能稳定发出。

### 2. FIFO 内存重分配（次可疑）
`usbd_conf.c`，OTG FS 总 FIFO 仅 **320 words（1280 字节）**。
4 端点布局：`RX64 + EP0:64 + EP1~3:56×3 + EP4(resp):16 = 312 words`，余量仅 8 words。
**需确认**：加麦克风端点后，各 IN 端点 TxFIFO 是否够。FIFO 偏小 → `TransmitPacket` 写 FIFO 时 NAK/延迟 → DataIn 回调推迟 → 下个 SOF 时 LSM 端点仍 BUSY → 丢半缓冲。
（当前磁盘上的 3 端点源码 FIFO 注释却写"4 IN"，说明源码处于混合状态，请以真实新固件为准。）

### 3. 双缓冲内存布局
`usbd_wcid_app.c`：3 端点版 `s_tx_buf_lsm/h3/qma`；4 端点版多一个 `s_tx_buf_mic[2048×2+2]`。
**需确认**：新增麦克风缓冲是否与传感器缓冲存在地址重叠/越界，或挤占 RAM 导致行为异常。

---

## 五、固件诊断计数器佐证（新固件日志）

新固件曾打印：`overrun≈55k  sof_send≈1.4k  datain_complete≈1.4k  delta≈0`
- `sof_send` 仅理论值（~1887/20s）的 ~75%，且远低于"每 SOF 都发"的期望 → **LSM 端点大量 SOF 周期处于 BUSY，发不出去**。
- `delta≈0`（发出的都完成了）→ 不是 USB 链路问题，是**发送机会被卡**。
- 对照 #2 旧固件 0.001% → 这个"卡"是 4 端点改造引入的。

---

## 六、上位机侧备注

为本次对照测试，上位机加了**旧固件兼容开关**：
- `WinUsbDeviceManager.LegacyFirmwareMode`（环境变量 `LEGACY_FW=1` 启用）
- 启用后：不读 MIC 端点、响应端点用 0x84（旧固件布局）。

正式发布连**新固件**时，**不要**设 `LEGACY_FW`（保持 false），端点映射为 MIC=0x84 / RESP=0x85。

---

## 七、一句话总结

> **旧固件 3 端点 + 新上位机 = 0.001% 丢帧（满速）。问题在新固件 4 端点改造，请 diff 新旧固件 SOF 发送循环与 FIFO 分配。上位机/WinUSB/USB FS 均已排除。**
