# LSM frame_id 倒退（乱序）定位 — 证据指向固件 4 端点版

**日期**：2026-06-18
**结论**：倒退（乱序）根因在**固件 4 端点版的 LSM 双缓冲发送时序**，不是上位机读取。

---

## 一、固件团队的判断 vs 实测

固件团队认为是"上位机多线程/多 overlapped 并发回收乱序"。我们做了对照实验**反驳**了这一点。

### 关键对照表（倒退次数 = 真乱序，统计自 lsm_raw.bin 原始字节流）

| 录制 | 固件 | 上位机 overlapped | 真乱序倒退(<300帧) |
|------|------|------------------|-------------------|
| 235449 | **3 端点** | N=32 | **1** ✅ |
| 104511 | **3 端点** | N=32 | **2** ✅ |
| 163117 | 4 端点 | N=32 | 128 ❌ |
| 163532 | 4 端点 | N=32 | 154 ❌ |
| 195414 | 4 端点 | N=32 | 51 ❌ |
| 195817 | 4 端点 | **N=1（最串行）** | 40 ❌ |

### 三条反驳证据

1. **同一份上位机代码**：接 3 端点固件 = 1~2 次倒退（近乎完美），接 4 端点固件 = 40~154 次。
   唯一变量是**固件**，不是上位机。

2. **N=1 单请求仍倒退**：把 overlapped 改成"挂一个、等一个、再挂下一个"
   （`WaitForSingleObject` 严格串行，物理上上位机不可能乱序），4 端点固件下倒退仍有 40 次。
   → 上位机已串行到极限，乱序依旧 → 不是上位机并发问题。

3. **倒退在 lsm_raw.bin 里就存在**：raw.bin 是读线程 `onData` 之前"收到啥写啥"的原始字节，
   N=1 时读线程严格串行写入。raw 里有整块倒退 = WinUSB 交付给上位机的字节流本身已乱序。

---

## 二、倒退的具体形态（4 端点固件）

倒退是**整块 ~140 帧的错位**，规整复现。raw.bin 实例：

```
[2252] 69661,F60101000000,92.7,301.1,967.3,...      ← 帧 69661 完整
[2253] 93.0,303.3,962.8,280.0,2590.0,-2170.0        ← 半行(缺 frame_id 头)
[2254] 69521,F60101000000,89.1,306.1,963.2,...      ← 跳回 69521 (倒退 140)
[2255] 69522,...                                     ← 继续 69522,69523...
```

特征：
- **整块倒退 ~140 帧**（≈ 一个 LSM 半缓冲的帧数）。
- 倒退处伴随**半行**（一帧被从中间劈开，前半截和 frame_id 头分离）。
- 倒退后从 69521 继续递增 69522、69523…（说明那 140 帧数据都在，只是整块迟到、插到了 69661 之后）。

→ 像是固件**把"上一个半缓冲"与"当前半缓冲"的发送顺序发反了**，或半缓冲切换时被打断、跨缓冲拼接错位。

---

## 三、请固件团队排查方向

4 端点版相对 3 端点版，新增了 MIC（SAI DMA + 96kHz）。怀疑：

1. **MIC 的 SAI/DMA 中断抢占了 LSM 半缓冲切换的临界区**
   → LSM 在 `TxBuffStatus`/半缓冲索引更新到一半时被打断，导致发送的半缓冲指针错乱（发了上一块或发重了）。
   建议：LSM 半缓冲切换、`SetTxBuffer`、`TxBuffStatus` 更新这段加临界保护（关中断或用原子操作）。

2. **半缓冲"完成但未发"与"新完成"的竞争**（SOF 发送循环里 TxBuffStatus 1/2 的处理）
   4 端点后通道数变化，确认 LSM(ch0) 的半缓冲状态机在 MIC 中断干扰下不会错发。

3. **对照验证**：固件临时**禁用 MIC 的 SAI/DMA（彻底不启动，连中断都不开）**，
   只跑 4 端点拓扑（端点/FIFO 不变）测 LSM。
   - 若倒退归零 → 坐实是 MIC 中断/DMA 抢占 LSM 缓冲临界区。
   - 若仍倒退 → 是 4 端点拓扑/FIFO 本身的缓冲管理问题。

---

## 四、上位机侧现状

- LSM 读取：32 overlapped + 严格 FIFO 轮转回收（`WaitForSingleObject(evts[cur])`，cur 0..N-1 轮转）。
  这已是保序的正确实现；3 端点固件下实测倒退仅 1~2 次（近乎完美）。
- 上位机**不做重排**（不能假设 frame_id 排序，那会掩盖问题）；倒退如实反映在数据里。
- 注：上位机 H3/QMA 当前用 4 线程并发同步读（历史遗留），低速下未暴露问题，后续会改单线程。

---

## 五、一句话

> 同一上位机代码：3 端点固件倒退 1~2 次，4 端点固件倒退 40~154 次；N=1 最串行读仍倒退 40 次；
> 倒退在原始字节流 raw.bin 中即存在。**乱序来自 4 端点固件的 LSM 双缓冲发送，疑因 MIC 中断/DMA
> 抢占缓冲切换临界区。请固件禁用 MIC SAI/DMA 跑 4 端点测 LSM 验证。**
