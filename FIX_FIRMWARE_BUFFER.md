# 方案 C:固件增大 LSM 缓冲(临时缓解,治标不治本)

## 适用场景
若方案 A/B 短期做不了,需要**临时能用**(接受偶发丢帧,不是 0 丢)。

## 原理
当前 LSM 双缓冲 = **4096×2 = 8192 字节** ≈ **1.2 秒**(6664Hz × 58B/行 = 377KB/s → 8KB = 21ms,但每半缓冲 4KB ≈ 10ms,两个交替 ≈ 1.2s 容错窗口)。

增大到 **16KB×2 = 32KB** → 容错窗口 ≈ **5 秒**(主机偶尔卡顿 <5s 还能恢复)。但**主机持续慢(当前 96% SOF 等待)最终还是会溢出**,只是晚点。

## 固件改动(2 处)

### 1. `Core/Inc/usbd_wcid_app.h` 第 31 行
从:
```c
#define WCID_TX_HALF_SIZE  4096U
```
改成:
```c
#define WCID_TX_HALF_SIZE  16384U  /* LSM 半缓冲 16KB(容错窗口 ~5s) */
```

### 2. `Core/Src/usbd_conf.c` FIFO 重分配
当前 Tx FIFO 总共 = 64+56+24+24+56+16 = 240 words(不含 Rx 64)。
LSM 半缓冲从 4KB 涨到 16KB,但**硬件 FIFO 不需要跟着涨**(FIFO 只是传输层缓冲,双缓冲是应用层)。

**保持 FIFO 不变**即可(LSM 仍 56 words = 224B 硬件缓冲,够一个 bulk 包)。

## 编译后 RAM 占用
- LSM 缓冲增量:12KB×1 通道(其它 H3/QMA/MIC 不变)
- STM32F411 有 128KB RAM,够。

## 预期效果
- **主机偶发卡顿 <5s** → 缓冲吸收,不丢帧;
- **主机持续慢(当前状态:96% SOF 等待)** → 最终仍溢出,只是从 ~20ms 后溢出变成 ~5s 后溢出。

**所以这是临时缓解,不是根治**——根治必须靠方案 A/B 提升主机读取速度。

## 与方案 A/B 对比

| 方案 | 改动量 | 效果 | 适用 |
|---|---|---|---|
| **A:overlapped**(推荐先试) | 上位机 5 行 | 若成功 → sof_send 30k+,接近 0 丢 | 5 分钟,最快 |
| **B:libusb**(治本) | 上位机 ~150 行 + 驱动切换 | sof_send 30k+,接近 0 丢 | 1-2 天,根治 |
| **C:缓冲**(临时) | 固件 1 行 | 偶发卡顿 <5s 不丢;持续慢仍丢 | 临时能用,不是根治 |

**建议顺序**:先 A(5 分钟)→ 若不够再 B(治本)→ C 仅当 A/B 短期做不了时用。

---

## 三个方案总结

| 项 | 方案 A | 方案 B | 方案 C |
|---|---|---|---|
| 改哪里 | 上位机(1 行改 overlapped) | 上位机(libusb,~150 行) | 固件(缓冲 4K→16K) |
| 时间 | 5 分钟 | 1-2 天 | 10 分钟 |
| 预期 sof_send | 可能 30k+(若 WinUSB overlapped 够快) | 30k+(Linux 实测 0 丢) | 仍 ~1.6k(主机没变) |
| 预期丢帧率 | 可能 <1% | <1% | 仍 ~18%(只是晚点溢出) |
| 根治? | 可能(取决于 WinUSB 性能) | 是 | 否(临时) |

**我的建议**:
1. **立刻试方案 A**(5 分钟):启用现有 `ReadLoopOverlapped`,编译→测→看 `sof_send`;
2. **若 A 不够(sof_send 仍 <10k)**:做方案 B(libusb,1-2 天但治本);
3. **方案 C 别做**(除非 A/B 短期做不了且必须临时能用)——它不解决根因,只是延后溢出。

---

三个方案文档已写进 `USB-view/` 目录:
- `FIX_OVERLAPPED.md`:方案 A(5 分钟,推荐先试)
- `FIX_LIBUSB.md`:方案 B(1-2 天,治本)
- `FIX_FIRMWARE_BUFFER.md`:方案 C(临时,不推荐)

**现在让上位机先试方案 A,把结果(sof_send/overrun)贴给我。**
